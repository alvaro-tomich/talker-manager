const age = require('./validateAge');
const name = require('./validateName');
const rate = require('./validateRate');
const talk = require('./validateTalk');
const token = require('./validateToken');

module.exports = {
    age,
    name,
    rate,
    talk,
    token,
};